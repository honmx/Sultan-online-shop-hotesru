import React, { FC } from "react";
import s from "./CatalogItemPage.module.scss";

interface Props {
  
}

const CatalogItemPage: FC<Props> = ({  }) => {
  return (
    <div>
      catalog item page
    </div>
  )
};

export default CatalogItemPage;
